# Sources & build tooling (the shipped code is the bundled dist/extension.js)
src/**
build.mjs
.vscodeignore
.gitignore
node_modules/**
**/*.map

# Keep in the VSIX: dist/, media/, syntaxes/, package.json, README.md, LICENSE
